# Laboratorio 06 — copiar como .env y completar. El .env NO se sube a git (tiene la clave).
# Lo leen el notebook (celda de Configuración) y docker compose.

EQUIPO=equipo-1

# Stream compartido del VPS: puerto 443 (HTTPS), con TLS
VPS_HOST=redis-stream.5.75.154.51.sslip.io
VPS_PORT=443
VPS_PASS=

# Plan B — servidor de emergencia en el PC del profesor (sin TLS). Si el profesor lo levanta:
# VPS_HOST=<IP que dé el profesor>
# VPS_PORT=6379
